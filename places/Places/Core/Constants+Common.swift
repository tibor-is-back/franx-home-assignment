extension Constants {
    enum Common {
        enum Strings {
            static let invalidNumber = "Enter a valid number"
            static let outOfRange = "Value must be between %@ and %@"
        }
    }
}
